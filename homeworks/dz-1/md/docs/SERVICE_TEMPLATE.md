<!--
  Паспорт микросервиса (краткая версия).
  Плейсхолдеры <...> заменить. Комментарии <!-- ... --> в GitHub не рендерятся.
  Неприменимые разделы помечайте «N/A», а не удаляйте.
-->

# <Service Name>

> **TL;DR:** одно предложение — что делает сервис.

| | |
|---|---|
| **Bounded Context** | `<context>` |
| **Владелец** | `<team>` |
| **Статус** | `Active / Maintenance / Deprecated` |

---

## 1. Назначение и контекст

<!-- Какую задачу решает сервис и его место в ландшафте. 2–4 предложения. -->
<...>

---

## 2. Возможности

- **<Capability 1>** — <краткое описание>.
- **<Capability 2>** — <краткое описание>.

---

## 3. Границы

<!-- За что сервис НЕ отвечает / что вынесено за его пределы. Кратко. -->
- **In scope:** <...>.
- **Out of scope:** <...> (это делает `<кто/что>`).

---

## 4. Интеграционные интерфейсы

### 4.1. Предоставляемые

**Синхронные**

| Интерфейс | Протокол | Спецификация |
|---|---|---|
| `<REST API>` | HTTP/JSON | [OpenAPI](./api/openapi.yaml) |
| `<gRPC>` | gRPC | [`.proto`](./api/service.proto) |

**Асинхронные**

| Событие / канал | Транспорт | Спецификация |
|---|---|---|
| `<event>` | `<Kafka: topic>` | [AsyncAPI](./api/asyncapi.yaml) |

### 4.2. Потребляемые

| Зависимость | Стиль | Контракт |
|---|---|---|
| `<upstream-service>` | Sync REST | [OpenAPI](<url>) |
| `<event-source>` | Async | [AsyncAPI](<url>) |

---

## 5. Архитектура

<!--
  Архитектура сервиса — часть общей контейнерной диаграммы (C4 L2) в Structurizr.
  Дайте ссылку на представление, сфокусированное на этом сервисе, если оно есть;
  иначе — ссылку на общую диаграмму с указанием, какой контейнер искать.
-->
Контейнерная диаграмма (C4, Level 2): [Structurizr — представление сервиса](<url>)

> На общей диаграмме сервис представлен контейнером **`<service-name>`**.

---

## 6. Данные

<!-- TODO -->

---

## 7. Нефункциональные требования

<!-- TODO -->

---

## 8. Наблюдаемость

<!-- TODO -->
